name: C/C++ CI

on: [push]

jobs:
  build-windows:
    name: Windows
    runs-on: [windows-latest]
    
    steps:
    - uses: actions/checkout@v2
    - name: configure
      run: |
        mkdir build
        cd build
        cmake -DCMAKE_INSTALL_PREFIX=installWindows-Github ..
    - name: make
      run: |
        cd build
        cmake --build . -j
        cmake --build . --target install
    - name: test
      run: |
        cd build
        ctest
    - name: compress
      run: |
        ls build
        Compress-Archive -DestinationPath installWindows-Github.zip -Path build/installWindows-Github
    - name: Upload artifact
      uses: actions/upload-artifact@v1.0.0
      with:
        # Artifact name
        name: windows_install
        # Directory containing files to upload
        path: installWindows-Github.zip
      
  build-linux:
    name: Linux
    runs-on: [ubuntu-latest]
    
    steps:
    - uses: actions/checkout@v2
    - name: configure
      run: |
        mkdir build
        cd build
        cmake -DCMAKE_INSTALL_PREFIX=installLinux-Github ..
    - name: make
      run: |
        pwd
        cd build
        cmake --build . -j
        cmake --build . --target install
    - name: test
      run: |
        cd build
        ctest
    - name: compress
      run: |
        ls build
        tar -czvf installLinux-Github.tar.gz build/installLinux-Github
    - name: Upload artifact
      uses: actions/upload-artifact@v1.0.0
      with:
        # Artifact name
        name: linux_install
        # Directory containing files to upload
        path: installLinux-Github.tar.gz

