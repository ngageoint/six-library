import os
import Options

def set_options(opt):
    opt.tool_options('javaw')

def configure(conf):
    conf.check_tool('javaw')

def build(bld):
    bld.add_subdirs('nitf')

def distclean(context):
    pass
