def build(bld):
    #bld.add_subdirs('jasper-plugins libjpeg-plugins')
    bld.add_subdirs('libjpeg-plugins')

def distclean(context):
    #context.recurse('jasper-plugins libjpeg-plugins')
    context.recurse('libjpeg-plugins')
