def configure(conf):
    conf.sub_config('drivers jasper-plugins openjpeg-plugins')

def build(bld):
    bld.add_subdirs('drivers jasper-plugins libjpeg-plugins openjpeg-plugins')

def distclean(context):
    context.recurse('drivers jasper-plugins libjpeg-plugins openjpeg-plugins')
