import os
from os.path import join, exists
from waflib import Scripting, Options

def options(opt):
    opt.recurse()

def configure(conf):
    conf.recurse()

def build(bld):
    bld.recurse()

def distclean(context):
    context.recurse()
