NAME            = 'str'
VERSION         = '1.0'
MODULE_DEPS     = 'types except coda_oss'
UNITTEST_DEPS   = 'sys std'

options = configure = distclean = lambda p: None

def build(bld):
    bld.module(**globals())
