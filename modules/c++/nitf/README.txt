This library wraps the original C library (located in projects/nitf).  It uses
a base Object class that contains a handle that performs reference counting on
the underlying C objects to ensure proper memory allocation/deallocation.
