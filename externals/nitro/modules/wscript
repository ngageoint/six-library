VERSION = '2.8'

def options(opt):
    opt.recurse()

def configure(conf):
    conf.env['VERSION'] = VERSION
    conf.recurse()

def build(bld):
    if bld.env['LIB_TYPE'] == 'shlib':
        bld.recurse('drivers c')
    else:
        bld.recurse()

def distclean(context):
    context.recurse()
