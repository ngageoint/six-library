set(CODA-OSS_JARS_DIR "${CMAKE_CURRENT_SOURCE_DIR}" CACHE INTERNAL "")
install(DIRECTORY "${CODA-OSS_JARS_DIR}/"
        DESTINATION "${CODA_STD_PROJECT_LIB_DIR}/"
        FILES_MATCHING PATTERN "*.jar")
